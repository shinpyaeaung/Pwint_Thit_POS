export type ReceivingItem={id:string;product_name:string;sku:string;base_unit:string;expected_quantity:string;sellable_quantity:string;carton_size:string|null;tracks_expiry:boolean}
export type ReceivingShipment={id:string;shipment_number:string;version:string;warehouse_name:string;status:string;costs_finalized:boolean;items:ReceivingItem[]}
export type Receipt={id:string;receipt_number:string;shipment_id:string;shipment_number:string;warehouse_name:string;received_at:string;received_by:string;status:string;notes:string|null;items:(ReceivingItem&{received_quantity:string;damaged_quantity:string;missing_quantity:string;batch_number:string;batch_id:string;manufactured_on:string|null;expires_on:string|null;notes:string|null})[]}
export type Count={shipment_item_id:string;carton_size:string;received_cartons:string;received_units:string;damaged_quantity:string;batch_number:string;manufactured_on:string;expires_on:string;notes:string}
export type Stock={batch_id:string;warehouse_id:string;warehouse_name:string;product_name:string;sku:string;base_unit:string;batch_number:string;expires_on:string|null;version:string;sellable_quantity:string;available_quantity:string;reserved_quantity:string;damaged_quantity:string;carton_size:string|null;available_cartons:string|null;available_units:string;unit_cost_mmk?:string|null}
const scaled=(v:string)=>{if(!/^\d{1,14}(\.\d{1,6})?$/.test(v))throw Error();const[w,f='']=v.split('.');return BigInt(w+f.padEnd(6,'0'))}
const fixed=(n:bigint)=>{const neg=n<0n;const s=(neg?-n:n).toString().padStart(7,'0');return `${neg?'-':''}${s.slice(0,-6)}.${s.slice(-6)}`}
export function counts(row:Count,item:ReceivingItem){try{const cartons=scaled(row.received_cartons)*scaled(row.carton_size||'0');if(cartons%1000000n!==0n)return null;const received=cartons/1000000n+scaled(row.received_units),damaged=scaled(row.damaged_quantity),expected=scaled(item.expected_quantity),sellable=received-damaged;return{received:fixed(received),sellable:fixed(sellable),missing:fixed(expected-received),valid:received<=expected&&damaged<=received&&sellable===scaled(item.sellable_quantity)}}catch{return null}}
