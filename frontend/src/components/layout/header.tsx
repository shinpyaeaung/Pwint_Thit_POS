import { Menu } from 'lucide-react'
import { Button } from '@/components/ui/button'
import type { CurrentUser } from '@/permissions'
export function Header({ user, onMenu, onLogout, signingOut }: { user: CurrentUser; onMenu: () => void; onLogout: () => void; signingOut: boolean }) {
  return <header className="sticky top-0 z-20 flex min-h-16 flex-wrap items-center justify-between gap-3 border-b bg-white px-4 py-3 sm:px-7"><div className="flex items-center gap-3"><Button variant="ghost" size="icon-sm" className="lg:hidden" aria-label="Open navigation" onClick={onMenu}><Menu /></Button><span className="text-xs font-medium text-muted-foreground">Distribution workspace</span><span className="hidden border-l pl-3 text-[11px] text-muted-foreground sm:inline">MMK base currency</span></div><div className="flex items-center gap-3"><span aria-hidden="true" className="hidden size-8 items-center justify-center rounded-full border bg-muted text-xs font-semibold sm:flex">{user.display_name.slice(0, 2).toUpperCase()}</span><div className="max-w-36 truncate"><p className="truncate text-xs font-medium">{user.display_name}</p><p className="mt-0.5 text-[10px] text-muted-foreground">{user.role === 'SUPER_ADMIN' ? 'Super Admin' : 'Staff Admin'}</p></div><Button size="sm" variant="outline" disabled={signingOut} onClick={onLogout}>{signingOut ? 'Signing out…' : 'Sign out'}</Button></div></header>
}
